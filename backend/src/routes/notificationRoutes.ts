import express from 'express';

const router = express.Router();

// TODO: Implement notification routes
router.get('/', (req, res) => {
  res.send('Notification route placeholder');
});

export default router;
