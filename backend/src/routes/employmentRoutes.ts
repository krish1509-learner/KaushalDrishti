import express from 'express';

const router = express.Router();

// TODO: Implement employment routes
router.get('/', (req, res) => {
  res.send('Employment route placeholder');
});

export default router;
